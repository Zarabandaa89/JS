let number = [1, 2, 3, 4, 5, 6];//creamos una lista de 6 elementos

for (lista of number){ //creamos un ciclo for of que recorre la lista, lista es la variable que
    //almacena el valor de cada elemento en cada recorrido
    console.log(lista);//imprime el valor de la variable lista
}